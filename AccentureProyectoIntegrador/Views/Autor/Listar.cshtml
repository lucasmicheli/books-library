@model IEnumerable<AccentureProyectoIntegrador.Models.Autores>


<script>document.ready(function () {
        InputDeviceInfo.addClass("form-control");
    })</script>

<div class="text-right">
    @Html.ActionLink(" ", "Agregar", null,
    new
        {
        @class = "glyphicon glyphicon-plus",
        style = "font-size:1.5em"
    })
</div>
<table class="table table-hover table-striped">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre Autor</th>
            <th class="text-right"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var p in this.Model)
        {
            <tr>
                <td>@p.IdAutor</td>
                <td>@p.NombreAutor</td>
                <td class="text-right">
                    @Html.ActionLink(" ", "Editar", new { Id = p.IdAutor },
                    new
                    {
                        @class = "glyphicon glyphicon-pencil",
                        style = "font-size:1.5em"
                    })

                    &nbsp &nbsp &nbsp
                    @Html.ActionLink(" ", "Eliminar", new { Id = p.IdAutor },
                    new
                    {
                        @class = "glyphicon glyphicon-trash",
                        style = "font-size:1.5em"
                    })
                </td>
            </tr>
        }
    </tbody>
</table>
